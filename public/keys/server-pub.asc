-----BEGIN PGP PUBLIC KEY BLOCK-----
NOTE — PLACEHOLDER. Replace this file with the REAL server public key.

Generate the real keypair OFFLINE (air-gapped machine), then:
  1. Copy the PUBLIC key here            (public/keys/server-pub.asc)
  2. Copy the PUBLIC key to the bot      (bot/keys/server-pub.asc)
  3. Copy the PUBLIC + PRIVATE key to api/keys/ (private key never leaves
     the verification server; the private key file must NOT be committed)

See README.md → "Generate the PGP keypair" for the exact gpg commands.
This placeholder file intentionally fails openpgp.readKey() so that the
submit form cannot silently send unencrypted data before setup is done.
-----END PGP PUBLIC KEY BLOCK-----
